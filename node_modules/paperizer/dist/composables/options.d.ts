import { PaperizerOption } from '../types/types';
declare const useOptions: (options?: PaperizerOption) => {
    target: string;
    features: string;
    windowTitle: string;
    autoClose: boolean;
    bodyClass: string;
    autoPrint: boolean;
};
export default useOptions;
